/* 

Contributor(s): Githika Annapureddy

Description: This component handles the dropdown when the 3 dots of an environment or cluster are pressed.
             It handles renaming and deleting the cluster/environment. 

Parent Component(s): EnvironmentContainer

Child Component(s): none

*/

import "./Dropdown.css"

export default Dropdown = ( {handleDeleteEnvironment, handleDeleteCluster, id, env_or_cluster} ) => {
  
  //handleRename function
    //renames the environ

  return (
    <>
       {/* Shows/Creates the dropdown with rename and delete options. Return this so that 
       it is rendered by caller component. */}
    </>
  )
}

